<section>
  <div class="container">
    <div class="row">
      {{#each}}
      <div class="col-md-1">
        <p>
          <a href={{url}} class="thumbnail">
            <img src={{avatarUrl}} width="70" height="70" />
          </a>
        </p>
      </div>
      {{/each}}
    </div>
  </div>
</section>
